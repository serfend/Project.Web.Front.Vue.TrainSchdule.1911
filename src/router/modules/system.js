import Layout from '@/layout'

const systemRouter =
{
  path: '/system',
  hidden: true,
  component: Layout,
  name: 'System',
  meta: {
    title: 'default.system.title',
    icon: 'component'
  }
}

export default systemRouter
